# VERSION DE EMERGENCIA: VERSIÓN 1X
## FECHA DE CREACIÓN: Sábado, 21 de Febrero de 2026, 00:54:31

**ESTA ES UNA SOLA REGLA TEMPORAL DEL ENTORNO HOPS.**

### Definición:
Al momento de la creación de este documento, TODO el código front y back existente, su funcionalidad, elementos, objetos, y cálculos de la aplicación HOPS han sido guardados y empaquetados bajo el nombre **"Versión 1X"**. 

### Condición de Restauración (Trigger):
**SIEMPRE** que el usuario cite textualmente o haga referencia a la **"Versión 1X"** con intención de revertir daño o restaurar código:
1. **NO PROCEDERÁS** en automático a sobreescribir ni revertir los archivos.
2. Invocarás una consulta de **DOBLE PASO** con la siguiente pregunta exacta:
    > "¿Está seguro que desea devolver todo el código al estado de la Versión 1X?"
3. Solo si el usuario da su **DOBLE CONFIRMACIÓN** y responde afirmativamente, procederás a copiar/restaurar todo el código respaldado.

### Sobre el sistema de control de versiones (git):
*Nota: Mientras estabilizábamos, se generó un respaldo físico íntegro del proyecto (exceptuando dependencias `node_modules` y directorios temporales `.next`) en la carpeta `../HOPS_Version_1X` como mecanismo de salvavidas temporal, ya que Git no estaba inicializado.*

**Una vez hayamos creado e integrado completamente `.git` a la estructura del flujo de trabajo, eliminaremos para siempre esta regla aplicable a este workspace HOPS.**
